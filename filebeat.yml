filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /app/logs/backend.log

output.logstash:
  hosts: ["logstash:5044"]

logging.level: info
logging.to_files: true
logging.files:
  path: /tmp/filebeat-logs
  name: filebeat.log
  keepfiles: 7
  permissions: 0644 